--- # tasks/main.yml
- name: Install Linuxbrew
  ansible.builtin.shell: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  args:
    creates: /home/linuxbrew/.linuxbrew/bin/brew
  become: true
  become_user: "{{ simpleprod_user }}"

- name: Add brew to PATH in .bashrc
  ansible.builtin.lineinfile:
    path: /home/{{ simpleprod_user }}/.bashrc
    line: 'export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"'
    create: yes
  become: true
  become_user: "{{ simpleprod_user }}"

- name: Add brew to PATH in .zshrc
  ansible.builtin.lineinfile:
    path: /home/{{ simpleprod_user }}/.zshrc
    line: 'export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"'
    create: yes
  become: true
  become_user: "{{ simpleprod_user }}"